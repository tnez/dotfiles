---
name: run-checks
description: Run the project's Definition of Done checklist
disable-model-invocation: true
allowed-tools: Bash, Read, Glob
---

# Run Checks

Load and follow the harness-agnostic `run-checks` skill.

The source of truth is:

`~/.agents/skills/run-checks/SKILL.md`

Use the skill named `run-checks`. If the skill tool does not list it, read the
file above directly and follow its instructions.
