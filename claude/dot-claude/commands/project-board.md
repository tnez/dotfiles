---
description: Steward the current repo's GitHub PRs and issues
---

Load and follow the harness-agnostic `project-board` skill.

The source of truth is:

`~/.agents/skills/project-board/SKILL.md`

Use the skill named `project-board`. If the skill tool does not list it, read the file above directly and follow its instructions.

If $ARGUMENTS is non-empty, treat it as additional board-pass instructions:

$ARGUMENTS
