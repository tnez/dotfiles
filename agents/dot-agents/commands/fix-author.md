---
description: Fix commit authors on the current branch
---

# Fix Author

Load and follow the harness-agnostic `fix-author` skill.

The source of truth is:

`~/.agents/skills/fix-author/SKILL.md`

Use the skill named `fix-author`. If the skill tool does not list it, read the
file above directly and follow its instructions.

If $ARGUMENTS is non-empty, treat it as additional fix-author instructions:

$ARGUMENTS
