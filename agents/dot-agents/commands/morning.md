---
description: Build Dottie's morning briefing
---

# Morning

Load and follow the harness-agnostic `morning` skill.

The source of truth is:

`~/.agents/skills/morning/SKILL.md`

Use the skill named `morning`. If the skill tool does not list it, read the
file above directly and follow its instructions.

If $ARGUMENTS is non-empty, treat it as additional briefing instructions:

$ARGUMENTS
